FROM sonarqube:community

LABEL org.opencontainers.image.title="Hermes SonarQube Community"
LABEL org.opencontainers.image.description="SonarQube Community Edition for local Hermes static code analysis"

# Thin wrapper over the official image. Extra plugins (if any) can be baked in here:
# COPY plugins/*.jar /opt/sonarqube/extensions/plugins/
